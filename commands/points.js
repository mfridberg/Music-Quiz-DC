const Discord = require('discord.js');

module.exports = {
    name: 'points',
    description: 'Keep track of users points',
    execute(message, args,) {
        const guild = new Discord.Guild(client, client.guilds);
        console.log();
    }
}